function subs(a, b) {
  return a - b;
}

export default subs; //& default export means when we import it from any file, like index.mjs, we will get this thing by default that means even if we name it sth else,like biyog during import, JS will understand this we are referring to the subs function!!
