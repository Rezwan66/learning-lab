# RDBMS & SQL (PostgreSQL)

Comprehensive collection of SQL scripts for PostgreSQL, covering everything from basic queries to advanced database concepts.

## 📜 SQL Scripts

- **Basics**: `SELECT`, `INSERT`, `UPDATE`, `DELETE`, `CREATE/DROP`.
- **Advanced Querying**: `JOINS` (Inner, Left, Right, Cross, Natural), `GROUP BY`, `HAVING`.
- **Database Design**: Foreign Keys, Constraints, Column types.
- **Advanced Features**:
    - **Procedures & Functions**: Reusable logic.
    - **Triggers**: Automated actions.
    - **Indexing**: Performance optimization.
    - **Subqueries**: Nested queries for complex data retrieval.

## 🐘 Usage
You can execute these scripts using any PostgreSQL client (pgAdmin, DBeaver) or the `psql` command line tool.
